# Normalize files
* text=auto

# Normalize Markdown
*.md diff=Markdown

# Set SVG as main language of repository
*.svg linguist-detectable

# Sent binary files to LFS
*.svg binary filter=lfs diff=lfs merge=lfs -text
*.png binary filter=lfs diff=lfs merge=lfs -text
